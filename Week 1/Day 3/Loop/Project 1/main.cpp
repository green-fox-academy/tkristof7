#include <iostream>
#include <string>

int main(int argc, char *args[]) {
    std::string name = "I wont cheat on the exam ";
    for (int i = 0; i < 100; i++)
        std::cout << name << std::endl;


    // Create a program that writes this line 100 times:
    // "I won't cheat on the exam!"

    return 0;
}